# Session 0001 — Phase 0: Toolchain + baselines

Date: 2026-07-31 · Machine: SER9 (Ryzen AI 9 HX 370, **AMD Radeon 890M** integrated, 64 GB
RAM, Windows 11 Pro 26200) · Agent session, unattended after the readiness Q&A.

## 1. Outcome

The project has a git repository, a pinned toolchain, all four upstream/reference clones
with recorded pins, and an archived copy of PR #11886 that is now *more* complete than
`gh pr diff` alone (the two pre-compiled shader blobs were binary and had to be extracted
from the PR head commit separately).

Three of the five exit criteria are met outright. The two that are not produced the most
valuable findings of the session:

- **Upstream ghostty does not link `libghostty.dll` on Windows/MSVC.** `zig build test`
  passes completely (3061 pass / 55 skip / 0 fail), but `zig build -Dapp-runtime=none`
  fails at the link step. Two distinct blockers, diagnosed to the line and then reverted:
  `memset` collides with `libvcruntime`, and `termio/Exec.zig` references the POSIX
  `close`/`write` symbols that the MSVC CRT does not export. wintty carries fixes for
  both, and its `src/` differs from upstream in **98 files** — the Windows gap is
  materially larger than ADR 0004's patch list assumes.
- **Windows Terminal needs no VS 2022.** WT's own `common.build.pre.props` selects the
  **v145** toolset when `VisualStudioVersion >= 18.0`, and its README ships a winget
  config for VS 2026. Native C++ builds clean on the installed VS 2026 Community
  (conhost linked successfully). The real blocker was the missing **UWP C++ workload**,
  which the XAML/WinRT projects need via `OpenConsoleUniversalApp=true`.

wintty builds and runs a live `cmd.exe` session on this GPU through the
ghostty-engine/SwapChainPanel path — the behavioral reference the plan wanted, plus proof
that the composition path works on Radeon 890M.

## 2. Exit criteria

- [x] **This repo is a git repository with the docs committed.**
      `git init -b main`, `core.symlinks=true`; `CLAUDE.md` is staged as mode `120000`
      (a real symlink) and resolves natively under Windows — Developer Mode is enabled,
      so no fallback copy was needed. `.gitignore` excludes `ghostty/`, `terminal/`,
      `reference/`, `tools/`, and build output.

- [ ] **`libghostty` (upstream, unpatched) builds and its tests pass on this machine.**
      **Half met — reported unmet as worded.**
      - `zig build test` → **3061 passed, 55 skipped, 0 failed** (75/75 steps, ~4 min).
      - `zig build -Dapp-runtime=none` → **fails**, 91/94 steps succeed, the
        `ghostty-internal.dll` link step fails. See §5 DECISION-NEEDED 1 for the two
        root causes and the evidence.

- [ ] **Windows Terminal builds from source and launches.**
      **In progress at session end — reported unmet.** Restore works, the native tower
      builds clean on v145 (`Host.EXE.vcxproj` → exit 0, `OpenConsoleProxy.dll`,
      `ConhostV2Lib.lib`), but every `OpenConsoleUniversalApp` project fails
      `MSB8036: The Windows SDK version 10.0.17134.0 (or later) was not found` because
      the UWP C++ workload was absent. The VS Installer was launched against
      `terminal/.vsconfig` and was still installing when the session closed; the build
      has not been re-run since. See §5 DECISION-NEEDED 2.

- [x] **wintty runs a shell locally.**
      `zig build -Dapp-runtime=none` in `reference/wintty` → `ghostty.dll` (35.5 MB) +
      `ghostty-vt.dll` (5.6 MB); `dotnet build windows/Ghostty.sln /p:Platform=x64` →
      0 errors; `Wintty.exe` launches, stays up, and renders an interactive
      `C:\Users\darre>` `cmd.exe` prompt. Screenshot captured during the session.

- [x] **PR #11886 diff archived; upstream pins recorded in DESIGN.md.**
      `docs/research/pr-11886-d3d11.diff` (105 KB, 19 files) plus
      `docs/research/pr-11886-assets/{cell_vs,cell_ps}.cso` extracted byte-exact from the
      PR head. Pins and the toolchain table are now two new sections in `DESIGN.md`.

## 3. How to verify

```powershell
cd E:\src\winterm-ghostty

# 1. Repo + symlink
git log --oneline
git ls-files -s CLAUDE.md          # expect mode 120000

# 2. ghostty tests pass, DLL link fails
.\scripts\build-ghostty.ps1 -Test   # expect: 3061 passed, 55 skipped
.\scripts\build-ghostty.ps1         # expect: FAILS on lld-link duplicate symbol: memset

# 3. Windows Terminal (after the VS Installer finishes)
.\scripts\build-terminal.ps1 -Project conhost     # known good
.\scripts\build-terminal.ps1                      # WindowsTerminal.exe — the open item

# 4. wintty
.\reference\wintty\windows\Ghostty\bin\x64\Debug\net10.0-windows10.0.19041.0\Wintty.exe

# 5. Archive integrity
Get-Item .\docs\research\pr-11886-d3d11.diff, .\docs\research\pr-11886-assets\*.cso
```

## 4. Deviations

| # | Deviation | Reason |
|---|---|---|
| 1 | **VS 2026 Community / MSVC v145**, not "VS2022 + Windows SDK" as PLAN Phase 0 says. | VS 2026 is what the machine has, and WT's `common.build.pre.props` explicitly selects v145 for `VisualStudioVersion >= 18.0`. VS 2022 is not required by current WT main. PLAN's wording is stale; propose amending it (see §6). |
| 2 | Reference clones live in `reference/` (gitignored), a directory `DESIGN.md`'s layout does not name. | The layout section lists only `ghostty/`, `terminal/`, `harness/`, `scripts/`. Small call, noted per PROCESS rule 3; propose adding `reference/` to the layout at retro. |
| 3 | Zig installed **locally** under `tools/zig-x86_64-windows-0.16.0` rather than machine-wide. | Keeps the pin per-repo and reproducible; `scripts/zigenv.ps1` re-downloads it if missing. `tools/` is gitignored. |
| 4 | Added `.gitattributes` (`* text=auto eol=lf`, `*.diff -text`). | Git's default CRLF conversion would corrupt the `git format-patch` output that ADR 0004 requires this repo to carry in `ghostty-patches/`, and would have corrupted the archived `.diff` too. |
| 5 | Applied wintty's one-line `quirks_memset.zig` fix to the ghostty clone **as a diagnostic**, then `git checkout --` reverted it. | Needed to find out whether the memset collision was the *only* Windows link blocker. It is not (see §5). The clone is verified clean; no Phase 1 work was started. |
| 6 | Fetched PR #11886's head into the ghostty clone as tag `archive/pr-11886`, and extracted the two `.cso` blobs into `docs/research/pr-11886-assets/`. | `gh pr diff` renders binaries as "Binary files differ". The PR is **CLOSED**, so the plan's "it could disappear" concern is live; the durable copy has to be in this repo, not in the gitignored clone. |

## 5. Decisions needed

### DECISION-NEEDED 1 — Upstream ghostty needs a Windows-build patch before Phase 1's real work can start

`zig build -Dapp-runtime=none` on upstream `4d605bf` fails with two independent link errors:

```
error: lld-link: duplicate symbol: memset
    note: defined at src/quirks_memset.zig:105
    note: defined at libvcruntime.lib(memset.obj)
```
after which (with wintty's fix applied):
```
error: lld-link: undefined symbol: close    referenced by src/termio/Exec.zig:127,128,542
error: lld-link: undefined symbol: write    referenced by src/termio/Exec.zig:207
```

The first has a known one-line fix in wintty (`builtin.abi != .msvc` added to the `enabled`
condition in `quirks_memset.zig`) — small, obviously correct, and upstreamable on its own.
The second is structural: `termio/Exec.zig` calls POSIX `close`/`write` directly, and
wintty's version of that file is heavily rewritten (it also replaces `xev.Process` with a
`WaitForSingleObject` thread, because under ConPTY the child is already in ConPTY's job
object and the IOCP completion never fires).

**Why this matters beyond a build error:** ADR 0004's patch list is `init-wtf16`,
`platform-win32`, `d3d11-backend`, `dwrite-discovery`, `winkeys`, `termio-external`,
`iocp-fixes`. None of them is "make the DLL link at all on Windows". wintty's `src/`
differs from upstream in 98 files, which suggests a substantial `windows-build` patch has
to come *first* in the series, before `init-wtf16`.

**Recommendation:** add a `windows-build` patch as patch 0 of the ADR 0004 series, scoped
to exactly what the link needs (the memset guard + an `internal_os` fd shim for
`close`/`write`), harvesting from wintty (MIT). Defer the `xev.Process`/ConPTY rework — it
belongs with `iocp-fixes` and, under ADR 0006, WT owns the PTY so ghostty's `Exec` backend
is off the critical path anyway. This needs a decision because it changes an Accepted-ADR
artifact (the patch list and its order).

### DECISION-NEEDED 2 — Finish or re-scope the Windows Terminal build

The VS Installer was launched with `terminal/.vsconfig` (full component set: UWP C++,
ManagedDesktop, ARM64 + ARM64EC, MSIX packaging, .NET targeting packs) against the
existing VS 2026 Community install, after you freed space on C:. It was mid-install at
session end, so `WindowsTerminal.vcxproj` has not been rebuilt.

**Recommendation:** first action next session is `.\scripts\build-terminal.ps1`; if MSB8036
is gone, the criterion closes retroactively and Phase 4 is unblocked. No design decision is
involved — this is a "did the install finish" check. If it *didn't* resolve, the next step
is comparing installed packages against `.vsconfig` rather than guessing.

## 6. Learnings for the plan

1. **PLAN Phase 0 says "VS2022 + Windows SDK". That is wrong for current WT main** — it
   selects v145 under VS 2026 and its README's winget config installs VS 2026 Community.
   Amend the text to "Visual Studio 2026 (v145) with `terminal/.vsconfig` applied".
2. **Phase 3's shader-toolchain open question is answered.** PR #11886 hand-wrote HLSL and
   compiled with `fxc /T vs_5_0 | ps_5_0`, embedding the `.cso` via `@embedFile`. That is
   the SM 5.0 DXBC route, not `dxc`/DXIL, and it confirms the question's own suspicion.
   Both `fxc` and `dxc` are present in SDK 26100.8249. Recorded in
   `docs/research/pr-11886-assets/README.md`.
3. **Phase 1's readiness question "does #11886 match what the research claimed?" —
   answer: partially, and the gap matters.** The PR is explicitly *infrastructure only*.
   Its own `DirectX11.zig` header says: "Current status: infrastructure only — COM
   bindings, device lifecycle, swap chain management, and an instanced cell-grid pipeline.
   The GenericRenderer integration (Target, Frame, RenderPass, Buffer, Texture, Sampler,
   shaders) is planned for follow-up work." `DirectX11.zig` contains **no `pub fn` at
   all** — only re-exports. DESIGN.md calls #11886 the "D3D11 module base", which is fair,
   but PLAN Phase 1's "port the revived #11886 diff as the base" should not be read as
   "the GraphicsAPI contract comes for free". Phase 1 writes that contract itself.
4. **wintty is a much larger harvest source than the harvest map implies** (98 changed
   files under `src/`). Worth an explicit diff-review step at Phase 1 readiness to decide
   what is *infrastructure we must take* versus *product decisions we should not*.
5. **Attribution convention is now urgent, not Phase 9 background.** The memset fix would
   be the first harvested MIT line and it lands in Phase 1. Decide per-file header note vs
   `NOTICES` before that patch is written (PLAN Phase 9 lists this as a standing question).
6. **`gh pr diff` is not a sufficient archive** for any PR carrying binaries. If more PRs
   get archived later, fetch `refs/pull/N/head` and extract blobs.

## 7. Next-phase readiness gaps (Phase 1)

Phase 1's own open-questions block, answered where this session could answer it:

- **ADRs 0002 and 0004 flipped to `Accepted`?** Still `Proposed` — hard block per the
  readiness bar. 0004 additionally needs the `windows-build` patch decision above folded
  in before it is flipped.
- **Did Phase 0 archive the #11886 diff intact, and does a first read confirm the
  research?** Archived, yes, and now binary-complete. First read: **partially confirms** —
  see learning 3. Treat the GraphicsAPI implementation as Phase 1 work, not a port.
- **Fork remote strategy (private GitHub fork vs local-only)?** Unanswered — no technical
  input from this session. Still needs a call for backup/collaboration reasons.
- **WTF-16 entry point: `ghostty_init_w` vs platform-conditional argv?** Unanswered;
  still delegated to the agent. Nothing this session contradicts that delegation.

New gaps this session surfaced:

- **The `windows-build` patch (DECISION-NEEDED 1) has no home in the plan.** Phase 1
  cannot produce a working harness without it, and it is not in ADR 0004's order.
- **Phase 1's escalation trigger "WARP device creation fails on this machine" is untested.**
  The dev box is an integrated Radeon 890M; WARP validation will be done over RDP into
  this same machine (confirmed at readiness). No second physical config exists.
- **ARM64 cross-build** (a Phase 1 exit criterion) was not attempted. `scripts/build-ghostty.ps1
  -Target aarch64-windows-msvc` exists but is unexercised, and it cannot pass while the
  x64 DLL link is broken.
- **The appcontainer/MSIX probe** (Phase 1 exit criterion) needs the MSIX packaging
  components, which are part of the `.vsconfig` install started this session.
